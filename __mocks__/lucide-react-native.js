const React = require('react');

const createIcon = () => {
  const Icon = () => null;
  return Icon;
};

module.exports = {
  __esModule: true,
  ArrowLeft: createIcon(),
  ArrowRight: createIcon(),
  Mail: createIcon(),
  Lock: createIcon(),
  Facebook: createIcon(),
  Instagram: createIcon(),
};
